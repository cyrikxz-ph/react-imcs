// import { fromJS } from 'immutable';
// import { makeSelectCustomersPageDomain } from '../selectors';

// const selector = makeSelectCustomersPageDomain();

describe('makeSelectCustomersPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
